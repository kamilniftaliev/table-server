package helpers

// func SetWorkloadAmountForTeachers(teachers []*models.Teacher) []*models.Teacher {
// 	for i := 0; i < len(teachers); i++ {
// 		workload := teachers[i].Workload
// 		totalHours := 0

// 		for j := 0; j < len(workload); j++ {
// 			totalHours += workload[j].Hours
// 		}
// 		// teachers[i].WorkloadAmount = totalHours
// 	}

// 	return teachers
// }
