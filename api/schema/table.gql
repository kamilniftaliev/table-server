scalar DateTime

type Table {
  id: ID!
  title: String!
  slug: String!
  created: DateTime!
  lastModified: DateTime!
  subjects: [Subject]
  subjectsCount: Int
  teachers: [Teacher]
  teachersCount: Int
  classes: [Class]
  classesCount: Int
}
