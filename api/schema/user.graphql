type User {
  id: ID!
  name: String!
  username: String!
  password: String!
  table: Table!
}

type Token {
  token: String!
  expiresAt: Int!
}

type Query {
  user: User!
}

type Mutation {
  login(username: String!, password: String!): Token!
}
